# Character Designer - Advanced Integration Guide

## Интеграция с MakeHuman

### Концепция интеграции

Character Designer использует принципы из MakeHuman, но адаптирован для работы с AI-генерацией изображений. Вот ключевые отличия и возможности интеграции:

#### Что взято из MakeHuman:

1. **Система модификаторов (0.0 - 1.0)**
   - Все параметры нормализованы
   - Линейная интерполяция между состояниями
   - Модульная структура категорий

2. **Категории параметров:**
   - Basic (пол, возраст, вес, мышцы, рост)
   - Proportions (пропорции тела)
   - Face (черты лица)

3. **Философия дизайна:**
   - Простые параметры → сложные результаты
   - Сохранение и загрузка пресетов
   - Интерполяция между персонажами

#### Что адаптировано для ComfyUI:

1. **Текстовые промпты вместо 3D mesh**
   - Конвертация параметров в описание
   - Оптимизация для Stable Diffusion/FLUX

2. **Интеграция с воркфлоу:**
   - Совместимость с CLIP Text Encode
   - Поддержка ControlNet
   - Работа с LoRA

## Расширенные возможности

### 1. Создание библиотеки персонажей

```python
# Создайте базовый набор архетипов
archetypes = {
    "warrior": {
        "muscle": 0.8,
        "height": 0.7,
        "body_proportions": 0.6
    },
    "mage": {
        "muscle": 0.3,
        "age": 0.6,
        "head_size": 0.6
    },
    "rogue": {
        "weight": 0.3,
        "muscle": 0.5,
        "leg_length": 0.7
    }
}
```

### 2. Интерполяция персонажей

Используйте утилиту `interpolate_characters` для создания промежуточных персонажей:

```python
from utils.character_utils import interpolate_characters

# Смешайте воина и мага для создания боевого мага
warrior = load_preset("warrior")
mage = load_preset("mage")

battle_mage = interpolate_characters(warrior, mage, weight=0.5)
```

### 3. Генерация вариаций

Создавайте вариации одного персонажа для разнообразия:

```python
from utils.character_utils import create_character_variations

base_character = load_preset("hero_male")
variations = create_character_variations(
    base_character,
    num_variations=10,
    variation_amount=0.15
)
```

## Интеграция с другими инструментами

### С MakeHuman (экспорт/импорт)

Если вы хотите использовать реальные 3D модели из MakeHuman:

1. **Экспорт из Character Designer:**
   ```python
   # Параметры можно сохранить в JSON
   # Затем вручную настроить MakeHuman с этими значениями
   ```

2. **Импорт из MakeHuman:**
   ```python
   # Извлеките значения модификаторов из .mhm файла
   # Конвертируйте в формат Character Designer
   ```

### С ControlNet

```
[Character Designer] → [Character to Prompt]
                              ↓
[ControlNet OpenPose] → [CLIP Text Encode] → [KSampler]
         ↑
[Pose Reference Image]
```

### С IP-Adapter

```
[Character Designer] → [Character to Prompt] → [CLIP Text Encode]
                                                        ↓
[Reference Face Image] → [IP-Adapter] ────────→ [KSampler]
```

## Создание кастомных нод

### Добавление новых параметров

Чтобы добавить новые параметры (например, "skin_tone"):

1. Откройте `nodes/character_designer_node.py`
2. Добавьте в `INPUT_TYPES`:

```python
"skin_tone": ("FLOAT", {
    "default": 0.5, 
    "min": 0.0, 
    "max": 1.0, 
    "step": 0.01,
    "display": "slider",
    "label": "Skin Tone (Light to Dark)"
}),
```

3. Добавьте параметр в функцию `create_character`
4. Обновите структуру данных

### Создание специализированной ноды

Пример: Нода для этнических пресетов

```python
class EthnicPresetNode:
    @classmethod
    def INPUT_TYPES(cls):
        return {
            "required": {
                "ethnicity": (["european", "asian", "african", "latino"],),
                "variation": ("FLOAT", {"default": 0.5, "min": 0.0, "max": 1.0}),
            },
        }
    
    RETURN_TYPES = ("CHARACTER_DATA",)
    FUNCTION = "apply_ethnic_preset"
    CATEGORY = "character/designer"
    
    def apply_ethnic_preset(self, ethnicity, variation):
        # Базовые параметры для каждой этничности
        presets = {
            "european": {
                "face.nose_size": 0.5,
                "face.eye_size": 0.5,
                "face.face_shape": 0.5
            },
            "asian": {
                "face.nose_size": 0.4,
                "face.eye_size": 0.45,
                "face.face_shape": 0.45
            },
            # и т.д.
        }
        
        # Применить пресет с вариацией
        # ...
```

## Оптимизация промптов

### Стратегии генерации промптов

1. **Минималистичный подход:**
   ```python
   # Только ключевые характеристики
   "female, young adult, athletic build"
   ```

2. **Детальный подход:**
   ```python
   # Все параметры в деталях
   "photorealistic, highly detailed, female, young adult, athletic build, tall stature, long legs, large expressive eyes, well-defined features, full body portrait"
   ```

3. **Контекстуальный подход:**
   ```python
   # Добавление контекста и стиля
   "cinematic lighting, fantasy art style, female warrior, young adult, athletic muscular build, tall imposing stature, confident pose"
   ```

### Кастомизация конвертера

Создайте свой класс конвертера для специфических стилей:

```python
class AnimatedSeriesPromptConverter:
    def convert(self, character_data):
        # Специальная логика для анимационных сериалов
        if character_data['parameters']['basic']['age'] < 0.3:
            style = "cel-shaded, western animation style"
        else:
            style = "anime style, detailed linework"
        
        # Формируем промпт
        # ...
```

## Советы по производительности

### 1. Кэширование параметров

При работе с большим количеством персонажей:

```python
# Сохраняйте часто используемые комбинации
character_cache = {}

def get_or_create_character(params_hash):
    if params_hash not in character_cache:
        character_cache[params_hash] = create_character(params)
    return character_cache[params_hash]
```

### 2. Батч-генерация

Генерируйте несколько персонажей за раз:

```python
def batch_create_characters(params_list):
    characters = []
    for params in params_list:
        character = create_character(**params)
        characters.append(character)
    return characters
```

## Устранение проблем

### Проблема: Параметры не применяются

**Решение:** Проверьте, что все значения в диапазоне 0.0-1.0

### Проблема: Промпт не генерирует нужный результат

**Решение:** 
1. Попробуйте другой `detail_level`
2. Добавьте negative prompts
3. Используйте более конкретные стили

### Проблема: Пресеты не загружаются

**Решение:**
1. Проверьте путь к папке `presets/`
2. Убедитесь в валидности JSON
3. Перезапустите ComfyUI

## API Reference

### CharacterDesignerNode

```python
create_character(
    gender: float,       # 0.0 (female) - 1.0 (male)
    age: float,          # 0.0 (child) - 1.0 (elderly)
    weight: float,       # 0.0 (thin) - 1.0 (heavy)
    muscle: float,       # 0.0 (less) - 1.0 (more)
    height: float,       # 0.0 (short) - 1.0 (tall)
    # ... остальные параметры
    randomize: int = 0   # > 0 для случайной генерации
) -> Tuple[Dict[str, Any], str]
```

**Returns:**
- `character_data`: Структурированные данные персонажа
- `parameters_json`: JSON-строка параметров

### CharacterToPromptNode

```python
convert_to_prompt(
    character_data: Dict[str, Any],
    style: str,          # "realistic", "anime", "cartoon", "artistic"
    detail_level: str    # "basic", "detailed", "very_detailed"
) -> Tuple[str]
```

**Returns:**
- `prompt`: Текстовый промпт для генерации

## Roadmap

### Запланированные возможности:

- [ ] **Реальная интеграция с MakeHuman targets**
  - Импорт .target файлов
  - Применение морфинга к базовой mesh
  
- [ ] **3D Preview**
  - Простой 3D вьювер в ноде
  - Использование Three.js или PyOpenGL
  
- [ ] **Анимация параметров**
  - Keyframe система
  - Морфинг между персонажами во времени
  
- [ ] **AI-assisted generation**
  - Автоматическое определение параметров по описанию
  - Integration с LLM для генерации описаний
  
- [ ] **Clothing system**
  - Параметры одежды
  - Стилизация костюмов

## Лицензирование

При использовании концепций MakeHuman:

- **MakeHuman License:** AGPL для кода, CC0 для ассетов
- **Character Designer:** MIT License
- **Ваши пресеты:** Вы владеете правами на свои пресеты

Если вы планируете включить реальные ассеты из MakeHuman (models, textures, targets), убедитесь в соблюдении их лицензий.

## Заключение

Character Designer предоставляет мощный и гибкий инструмент для создания параметризованных персонажей в ComfyUI. Используя принципы MakeHuman и адаптируя их для AI-генерации, вы получаете лучшее из обоих миров: точный контроль над параметрами и мощь современных моделей генерации изображений.

Удачи в создании уникальных персонажей! 🎨✨
